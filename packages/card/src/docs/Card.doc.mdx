import { Story, Canvas } from '@storybook/addon-docs'

# Cards

Display a card with different atributes.

[Github repo](https://github.com/unicoderns/ui/tree/master/packages/card)

```html
<ui-card
  variant="primary"
  header="header"
  title="title"
  background="true"
  style="width: 18rem;"
  img-src="https://picsum.photos/600/300/?image=25"
  img-bottom="false"
  footer="footer"
></ui-card>
```

### Card text

This component is use to send text to the card using its corresponding CSS class

```html
<ui-card-text>
  Some quick example text to build on the card title and make up the bulk of the
  card's content.
</ui-card-text>
```

## Variants

Support multiple variants with and without background

<Canvas>
  <Story id="ui-bootstrap-card--primary" />
  <Story id="ui-bootstrap-card--secondary" />
  <Story id="ui-bootstrap-card--success" />
  <Story id="ui-bootstrap-card--danger" />
  <Story id="ui-bootstrap-card--warning" />
  <Story id="ui-bootstrap-card--info" />
  <Story id="ui-bootstrap-card--light" />
  <Story id="ui-bootstrap-card--dark" />
</Canvas>
<Canvas>
  <Story id="ui-bootstrap-card--border-primary" />
  <Story id="ui-bootstrap-card--border-secondary" />
  <Story id="ui-bootstrap-card--border-success" />
  <Story id="ui-bootstrap-card--border-danger" />
  <Story id="ui-bootstrap-card--border-warning" />
  <Story id="ui-bootstrap-card--border-info" />
  <Story id="ui-bootstrap-card--border-light" />
  <Story id="ui-bootstrap-card--border-dark" />
</Canvas>

### Titles and text

<Canvas>
  <Story id="ui-bootstrap-card--titles-and-text" />
</Canvas>

### Images on top and bottom

<Canvas>
  <Story id="ui-bootstrap-card--image-top" />
  <Story id="ui-bootstrap-card--image-bottom" />
</Canvas>

### Header and footer

<Canvas>
  <Story id="ui-bootstrap-card--header-and-footer" />
</Canvas>

## Props

| Name         | Type      | Description                                       |
| ------------ | --------- | ------------------------------------------------- |
| `variant`    | `string`  | variant class defined in the theme                |
| `background` | `boolean` | will use variants with background if true         |
| `style`      | `string`  | allows to change card's size                      |
| `title`      | `string`  | allows to add a title to the card                 |
| `subtitle`   | `string`  | allows to add subtitle to the card                |
| `header`     | `string`  | allows to add header to the card                  |
| `footer`     | `string`  | allows to add header to the card                  |
| `imgSrc`     | `string`  | allows to add a image to the card                 |
| `imgAlt`     | `string`  | allows to add an alternative text to card's image |
| `imgBottom`  | `boolean` | card will show the image at the bottom if true    |
| `centerText` | `boolean` | center all text inside the card                   |

## Slots

| Name      | Description       |
| --------- | ----------------- |
| `default` | card text content |

# Theme

Use the next `json` to define a new theme for the component

```json
{
  cssClass: {
    main: string, // main class
    variants: { // define a custom list of variants
      'variant1': string,
      'variant2': string,
      ...
      'variantN': string,
    },
  },
}
```

Check the page [what is a theme](/docs/docs-theming--page#what-is-a-theme) to see how to use this json
