import { Story, Canvas, Preview } from '@storybook/addon-docs';

# Alerts
Display an alert with text to users.

[Github repo](https://github.com/unicoderns/ui/tree/master/packages/alert)

```html
    <ui-alert :="args" :show="toggle" @show="show" @close="close" @hide="hide">{{ args.default }}</ui-alert>
```

## Variants

Alerts support multiple variants

- [Primary](?path=/docs/ui-bootstrap-alert--primary&args=#primary)
- [Secondary](?path=/docs/ui-bootstrap-alert--secondary&args=#secondary)
- [Success](?path=/docs/ui-bootstrap-alert--success&args=#success)
- [Danger](?path=/docs/ui-bootstrap-alert--danger&args=#danger)
- [Warning](?path=/docs/ui-bootstrap-alert--warning&args=#warning)
- [Info](?path=/docs/ui-bootstrap-alert--info&args=#info)
- [Light](?path=/docs/ui-bootstrap-alert--light&args=#light)
- [Dark](?path=/docs/ui-bootstrap-alert--dark&args=#dark)
- [Inverted](?path=/docs/ui-bootstrap-alert--inverted&args=#inverted)
- [Dismissible](?path=/docs/ui-bootstrap-alert--dismissible&args=#dismissible)
- [Animated](?path=/docs/ui-bootstrap-alert--animated&args=#animated)

### Primary
<Canvas>
  <Story id="ui-bootstrap-alert--primary"/>
</Canvas>

### Secondary
<Canvas>
  <Story id="ui-bootstrap-alert--secondary" />
</Canvas>

### Success
<Canvas>
  <Story id="ui-bootstrap-alert--success" />
</Canvas>

### Danger
<Canvas>
  <Story id="ui-bootstrap-alert--danger" />
</Canvas>

### Warning
<Canvas>
  <Story id="ui-bootstrap-alert--warning" />
</Canvas>

### Info
<Canvas>
  <Story id="ui-bootstrap-alert--info" />
</Canvas>

### Light
<Canvas>
  <Story id="ui-bootstrap-alert--light" />
</Canvas>

### Dark
<Canvas>
  <Story id="ui-bootstrap-alert--dark" />
</Canvas>

### Inverted

Use the `invert` prop to alternate styles 
<Canvas>
  <Story id="ui-bootstrap-alert--inverted" />
</Canvas>

### Dismissible

Use the `dismissible` prop to add a close icon
<Canvas>
  <Story id="ui-bootstrap-alert--dismissible" />
</Canvas>

### Animated

Use the `animate` prop to animate an alert with a fade effect
<Canvas>
  <Story id="ui-bootstrap-alert--animated" />
</Canvas>

## Props
| Name | Type  | Description |
| ---- | ---- | ----------- |
| `animate` | `boolean` | add animate class to alert |
| `dismissible` | `boolean` | show close button |
| `show` | `boolean` | show/hide the alert |
| `variant` | `string` | variant class defined in the theme |
| `invert` | `boolean` | inverts the close button |


## Slots
| Name | Description |
| ---- | ----------- |
| `default` | actual alert content |

## Events
Alert will emit this events while changing state

| Event | Params | Desription |
| ---- | ---- | ----------- |
| show | NONE | Will only raise after transition |
| close | NONE | Will only when closing |
| hide | NONE | Will only after closing animation |


## Accessibility
Use the following properties to change the default accessibility text

| Name | Property | Default |
| ---- | ---- | ----------- |
| role | aria:role | `alert` |
| buttonClose | aria:buttonClose | `close` |

## Theme
Use the next `json` to define a new theme for the component

```json
{
  cssClass: {
    main: string // main alert class
    dismissible: string // class for dismissible state
    animated: string // class for animated alerts
    variants: KeyPairString // define a custom list of variants
    components: {
      buttonClose: string // class for button close
    }
  }
}
```

Check the page [what is a theme](/docs/docs-theming--page#what-is-a-theme) to see how to use this json


## Inner components

Note that the Alert uses [UiTransition](?path=/docs/ui-bootstrap-transition-default&args=#transitions) and [UiButtonclose](?path=/docs/ui-bootstrap-closebutton--default&args=#close-button) components internally, so when providing a theme you should provide for them as well.
