import { AlertMessageAccessibilityConfigModel } from '../../components'

export const alertMessageAccessibilityDefaults: AlertMessageAccessibilityConfigModel = {
  role: 'alert',
  buttonClose: 'Close'
}
